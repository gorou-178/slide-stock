# PUT /stocks/:id/memo 実装

メモ作成・更新エンドポイント PUT /api/stocks/:id/memo を実装してください。

## 前提

- docs/memo-api-spec.md（T-540 で作成）の仕様に従う
- T-502（認証ミドルウェア）が実装済みであること
- T-543 の QA テストが先に作成されている前提（TDD）

## やること

1. PUT /api/stocks/:id/memo を実装する:
   - パスパラメータから stock ID を取得する
   - stock の所有権を確認する
   - リクエストボディから memo_text を取得する
   - memos テーブルに upsert する（INSERT OR REPLACE）
   - 200 + memo オブジェクトを返す

2. `npm test` で全テストがパスすることを確認する

3. 変更をコミットする
