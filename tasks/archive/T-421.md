# 本番デプロイ確認（slide-stock.gorou.dev）

本番環境（slide-stock.gorou.dev）へのデプロイ結果を確認してください。

## 前提

- T-410, T-420 が完了していること
- デプロイが実行済みであること

## やること

1. 以下の基本動作を確認する:
   - `https://slide-stock.gorou.dev` にアクセスできること
   - トップページが表示されること
   - HTTPS でアクセスできること

2. API の動作確認:
   - `/api/health` が `{"status":"ok"}` を返すこと
   - 認証が必要なエンドポイントが 401 を返すこと

3. セキュリティ確認:
   - TEST_MODE が無効であること（test-auth-bypass が動作しないこと）
   - HTTP → HTTPS リダイレクトが機能すること
   - 適切な CORS ヘッダーが返ること

4. 確認結果を docs/deploy-checklist.md に記録する
   - 各項目のパス/フェイル
   - 問題があれば修正タスクを tasks/TASKS.md に追加する
