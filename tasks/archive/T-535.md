# worker/index.ts ルーティングリファクタ

現在の if/else ルーティングをハンドラマップ方式にリファクタリングしてください。

## 前提

- worker/index.ts は現在 `if (url.pathname === '/api/health')` のような分岐を使用している
- 今後 8+ エンドポイント（パスパラメータ含む）に対応する必要がある
- 既存のテストが全てパスする状態を維持する

## やること

1. worker/index.ts のルーティングをリファクタリングする:
   - URL パターンマッチング（パスパラメータ :id 対応）
   - HTTP メソッド × パスのハンドラマップ
   - 404 のデフォルトハンドラ
   - 例: URLPattern API または簡易ルーター関数

2. 既存エンドポイント（/api/health, /api/me）が同じ動作を維持することを確認する

3. `npm test` で全テストがパスすることを確認する

4. 変更をコミットする
